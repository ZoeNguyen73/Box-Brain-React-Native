import { View, Text, FlatList } from "react-native";
import React from "react";

const StackCard = ({ activeStack, stack}) => {
  return (
    <>
    </>
  )
};

const StackList = ({ stacks }) => {
  return (
    <View>
      <Text>StackList</Text>
    </View>
  )
};

export default StackList;